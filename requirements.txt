# LangChain SMC Trading Agent — Standalone Version
# ติดตั้ง: pip install -r requirements.txt

# SMC Indicators
smartmoneyconcepts>=1.0.4

# MetaTrader5 (Windows only — comment ออกถ้าใช้ Mac/Linux)
MetaTrader5>=5.0.45

# ⚠️ LangChain ต้อง pin เป็น 0.3.x
# เพราะ LangChain 1.x ย้าย AgentExecutor ออกไปแล้ว
langchain==0.3.25
langchain-openai==0.3.12
langchain-core==0.3.83
openai>=1.0.0

# Data
python-dotenv>=1.0.0
pandas>=2.0.0
numpy>=1.26.0

# Visualization
matplotlib>=3.7.0

# Jupyter
jupyter>=1.0.0
ipykernel>=6.0.0
